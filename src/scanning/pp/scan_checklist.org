#+TITLE: pp HighEGJet Trigger Scan Checklist
#+DESCRIPTION: Produce one scan output per jet trigger threshold for stitching

Goal: produce one scan output per jet trigger threshold so the spectra can be
stitched together in place of the old ZeroBias/MinBias sample.

Stitch order (lowest → highest): Jet15 → Jet30 → Jet40 → Jet60 → Jet80 → Jet100

* config_pp.h settings for each scan

For each scan, set exactly ONE ~applyJetXXTrigger = true~; all others false.
Keep: ~doHighEGJetSample = true~, ~fillMu12 = true~, ~doWDecayFilter = true~,
~doJetTrkMaxFilter = true~, ~deltaR = 40~ (epsilon_mm = 0.40).

* Scans

** DONE Jet15 — ~applyJet15Trigger = true~
- Output: ~pp_HighEGJet_Jet15HLT_mu12_pTmu-15to999_tight_deltaR-40_jetTrkMaxFilter_WDecayFilter_2026-6-15.root~

** DONE Jet30 — ~applyJet30Trigger = true~
- Output: ~pp_HighEGJet_Jet30HLT_mu12_pTmu-15to999_tight_deltaR-40_jetTrkMaxFilter_WDecayFilter_2026-6-16.root~

** IN-PROGRESS Jet40 — ~applyJet40Trigger = true~
- Currently configured and running
- Create EOS output dir before submitting

** TODO Jet60 — ~applyJet60Trigger = true~
- Note: existing file missing WDecayFilter — re-scan needed

** TODO Jet80 — ~applyJet80Trigger = true~
- Note: existing file missing WDecayFilter — re-scan needed

** DONE Jet100 — ~applyJet100Trigger = true~
- Output: ~pp_HighEGJet_Jet100HLT_mu12_pTmu-15to999_tight_deltaR-40_jetTrkMaxFilter_WDecayFilter_2026-6-16.root~

* Post-scan steps

** TODO hadd per-file outputs → one merged root file per trigger
** TODO copy/symlink merged files into ~rootFiles/scanningOutput/pp/latest/~
** TODO update ~plotJetPt_stitch.C~ to use new Jet15/30/40 files and remove MinBias
** TODO update ~calculateRAA.C~ stitch logic accordingly
